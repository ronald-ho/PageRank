// COMP2521 Assignment 2

// Written by:
// Date:

#include <assert.h>
#include <ctype.h>
#include <math.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "readData.h"

int main(int argc, char *argv[]) {


    // // If given wrong number of arguments
    // if (argc < 3) {
    //     fprintf(stdout, "Usage: %s [2 or more .txt files]\n", argv[0]);
    // }

    // // Get the texts from command line and put them into a struct
    // Collection texts = getText(argc, argv);

    // // Get the urls from the text files given in command line
    // getUrlFromText(texts);

    // // Create Collection to store all unique urls and the average index
    // Collection uniqueUrls = getUniqueUrls(texts);

    // // Create an array for permutation
    


    // // Output the url list
    

}